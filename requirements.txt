pyyaml>=6.0
jsonpatch>=1.33
jsonpointer>=2.4
pyarrow>=17.0
pandas>=2.2
numpy>=1.26
tqdm>=4.66
matplotlib>=3.9
httpx>=0.27
pytest>=8.0

# Used only by finetune/export_for_unsloth.py, to render the real chat
# template + count exact tokens. Not needed for dataset/ or generation/.
# The actual training stack (torch, unsloth, trl, ...) lives in
# finetune/requirements.txt, installed into its own finetune/.venv instead
# of here -- see that file for why.
transformers>=4.44
jinja2>=3.1
